{
  "name": "bbcigars-sites",
  "version": "1.0.0",
  "private": true,
  "description": "BBCigars monorepo containing multiple business sites and applications",
  "workspaces": [
    "renaissance-atlanta",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "rm -rf node_modules && pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}